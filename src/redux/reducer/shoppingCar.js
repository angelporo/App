import {Map, fromJS} from 'immutable';
import user from '../state/userState';// 商品详情


// type


/**
 * 购物车reducer
 * Param: param
 * Return: { new state }
 **/
export default function (state = user, action) {
  switch (action.type) {
  default:
    return state;
  }
}
